# Repos mounted into the devbox, one host path per line.
# Each becomes /code/<basename>. Nothing else on the host is visible.
# ~ is expanded, # starts a comment.

~/GitHub/flashcards
~/GitHub/kaiser2
